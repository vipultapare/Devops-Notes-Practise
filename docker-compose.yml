version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${DOCKER_USERNAME}/devops-web:${IMAGE_TAG}

    container_name: ${CONTAINER_NAME}
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - PYTHONUNBUFFERED=1
    restart: always
